# SignalBoard

독립 프로젝트 폴더입니다.

목표:

- 네이버부동산 저장 검색 URL 기반 신규 매물 감지
- 카카오톡 나에게 메시지 발송
- 이후 웹 UI와 모바일앱으로 확장 가능한 API-first 구조 유지

채널 이름:

- `부동산알리미`

초기 구조:

- `app/` : 백엔드 코드
- `assets/` : 브랜딩/채널 이미지
- `.env.example` : 환경변수 예시

다음 구현 범위:

1. 저장 검색 URL 등록
2. 신규 매물 감지
3. 카카오 알림 발송
4. 최소 API + 웹 관리 화면
